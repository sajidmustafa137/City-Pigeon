local function initialize(world)
    
end

local function update()
    
end

local function draw()
    
end

return {
    initialize = initialize,
    update = update,
    draw = draw,
}